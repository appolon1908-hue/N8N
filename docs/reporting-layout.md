# Reporting layout

The daily digest renders normalized JSON, CSV-compatible rows, plain text, and email-safe HTML. It contains KPI cards, campaign and agent tables, callback SLA, AI/QA, automation health, and data-integrity warnings. Sprint 1 performs no delivery.
