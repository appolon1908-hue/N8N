# Workflow catalog

The authoritative catalog is `manifests/workflow-manifest.json`. IDs are deterministic and workflow artifacts are grouped under `workflows/shared`, `events`, `callbacks`, `intelligence`, `alerts`, `reporting`, and `errors`.
